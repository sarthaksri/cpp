#include <iostream>
#include <vector>
using namespace std;

vector<int> nqueen(vector<vector<int>>board)
{
    
}
int main()
{
    int n;
    cout<<"Enter the board size: ";
    cin>>n;
    vector<vector<int>>board(n);
    vector<int>ans = nqueen(board);
}